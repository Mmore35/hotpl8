# Example policies

All examples monitor only. Claude slot numbers and Codex account paths are fictional; replace them locally. Never copy native credentials into these files. See [configuration](../docs/configuration.md).
